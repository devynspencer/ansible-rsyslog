# -*- mode: ruby -*-
# vi: set ft=ruby :

Vagrant.configure 2 do |config|
  config.vm.box = 'bento/centos-7.1'
  config.vm.hostname = 'test01.rsyslog.example.com'
  config.vm.network 'private_network', ip: '192.168.33.11'
end
